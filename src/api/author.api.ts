export type Author = {
  _id: string;
  name: string;
  email: string;
  createdAt: string;
  updatedAt: string;
  uid: string;
  avatar: string;
};

const name = "author";

const authorApi = {};

export default authorApi;
